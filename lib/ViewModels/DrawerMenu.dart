import 'package:flutter/cupertino.dart';

class DrawerMenu {
  String title;
  String subTitle;
  String icon;
  Widget destination;

  DrawerMenu(this.title, this.subTitle, this.icon, this.destination);
}